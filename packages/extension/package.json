{
  "name": "@onegov/extension",
  "version": "0.1.0",
  "private": true,
  "description": "Chrome MV3 extension shell — background service worker, content script, popup UI. The only package allowed to touch chrome.* / browser.*.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite build --watch",
    "test": "bun test",
    "typecheck": "tsc --build"
  },
  "dependencies": {
    "@onegov/core": "workspace:*",
    "@onegov/ui": "workspace:*",
    "preact": "^10.24.2"
  }
}
